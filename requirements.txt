numpy>=1.26
pandas>=2.1
requests>=2.31
Pillow>=10.0
scikit-learn>=1.3
pytest>=7.4

# perceptual-quality category (02/03/04) -- heavy ML deps, CPU-only works
# but is slow; a CUDA-enabled torch install is much faster if you have a GPU
torch>=2.1
pyiqa>=0.1.11
open_clip_torch>=2.24
huggingface_hub>=0.20

# SigLIP category (11-13) -- google/siglip-base-patch16-224 via transformers
transformers>=4.40

# faces category (14-15) -- YuNet face detection
opencv-python>=4.8

# depth category (17) -- MiDaS_small via torch.hub (fetches its own weights;
# no new pip package -- timm, its one extra dependency, already comes in
# transitively via open_clip_torch above)

# saliency category (18) -- BLOCKED, see docs/RESULTS.md "Saliency": loading
# MSI-Net's legacy TF SavedModel hard-crashes (libprotobuf FATAL) with at
# least some tensorflow/protobuf version combinations. Install at your own
# risk until that's resolved.
tensorflow>=2.16

# pose category (19) -- YOLOv8n person detection; ViTPose itself is served
# through transformers (already a dependency, SigLIP category)
ultralytics>=8.3

# only needed if you pass --posters-s3-bucket (optional, see utils/posters.py)
# boto3>=1.34
